using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunScript : MonoBehaviour
{
/*
    private GameObject fishShot;
    private Rigidbody2D gunRb;
    private SpriteRenderer gunRender;

    [SerializeField]
    private Transform shootPoint;

    public void Start(){
        gunRb = this.GetComponent<Rigidbody2D>();
        gunRender = this.GetComponent<SpriteRenderer>();
    }
    public void display(Vector3 direction){

        fishShot.get
        if(direction.y > 0f)
            gunRender.sortingOrder  = 1;
        else if(direction.y < -1f)
            gunRender.sortingOrder = 3;

        gunRb.rotation = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
    }

    public void fire(){
        Instantiate(fishShot, shootPoint.position, Quaternion.identity);
    }
    */

}
